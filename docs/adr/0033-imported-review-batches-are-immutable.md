# Imported Review Batches Are Immutable

Once a Review Batch is imported, the batch becomes immutable: candidate decisions, duplicate-group membership, merge metadata, and close/import actions cannot be changed through the batch review workflow. Later corrections belong to post-import edit, archive, or value-history flows so active Project Memory is not rewritten by reopening the original import decision.
