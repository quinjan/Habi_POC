# Poll Processing Job List Every Two Seconds

The frontend will poll the selected Project Workspace Processing Job list every two seconds in the first async source-processing UI. This interval is responsive enough for local POC review while keeping backend request volume modest and easy to tune later.
