# Reviewers Control Duplicate Candidate Groups

The POC will let AI propose Duplicate Candidate Groups during extraction and also let reviewers create or modify groups during review. Merge actions are valid only within a Duplicate Candidate Group, which prevents arbitrary unrelated records from being merged while still allowing reviewers to fix missed, incorrect, or newly noticed duplicate relationships before import.
