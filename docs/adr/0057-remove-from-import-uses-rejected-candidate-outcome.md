# Remove From Import Uses Rejected Candidate Outcome

When a reviewer removes an affected candidate from import while resolving a Taxonomy Gate, Habi will use the existing Rejected Candidate outcome rather than adding a separate taxonomy-specific removal action. Removing a candidate from import is a candidate review outcome, while approving, mapping, or rejecting an AI-suggested category path remains a Taxonomy Decision.
