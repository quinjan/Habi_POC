# Taxonomy Gates Apply To All Imported Memory Records

Every imported Memory Record must have a Resolved Category Path, regardless of record type. Purchase Lines may have additional import gates such as a required linked Material or Service concept, but Taxonomy Gates are shared across Materials, Services, Providers, Purchase Lines, and later active project memory types.
