# Visible AI Candidates Require Complete Taxonomy

AI Extraction will create visible Extracted Candidates only when the candidate has a complete two-level taxonomy suggestion that can default the reviewed category fields. This trades off surfacing every plausible purchase-line extraction in favor of keeping the Review Batch importable without unresolved taxonomy gates; omitted possible lines are represented through Processing Job diagnostics rather than incomplete candidate rows.

