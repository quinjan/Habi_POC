# Taxonomy Defaults Auto-Fill Reviewed Category Fields

When Habi has a project-local taxonomy default from an approved or mapped Taxonomy Decision, the review UI may auto-fill the candidate's reviewed category fields with that Resolved Category Path. The UI must disclose the default's provenance, and the reviewer may still change the category before approval; auto-filled taxonomy does not bypass candidate approval or import readiness checks.
