# Backend-Enforced Import Guards

Import rules for active Project Memory will be enforced in the backend even when the frontend also validates or guides the reviewer. The first manual-source import slice must reject imports that do not satisfy project scoping, review completeness, approval, source evidence, resolved category path, Material-or-Service linkage, and non-duplicate batch import requirements because the backend owns the trust boundary into active memory.
