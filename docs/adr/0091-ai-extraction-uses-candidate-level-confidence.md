# AI Extraction Uses Candidate Level Confidence

The first AI Extraction slice will store one candidate-level confidence score for each extracted Purchase Line candidate. Field-level confidence is deferred because it would add schema, validation, and review UI complexity before Habi has proven that candidate-level confidence is enough to guide reviewer attention.
