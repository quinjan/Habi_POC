# Existing Taxonomy Path Suggestions Do Not Require Taxonomy Gates

When AI suggests a category path that already matches an approved Taxonomy Node path inside the selected Project Workspace, Habi should treat the candidate's category path as resolved and should not require a Taxonomy Gate. Taxonomy Decisions are required for AI-suggested taxonomy changes, while ordinary classification to an existing path remains part of candidate review and import readiness.
