# Use One Manual Source Entry Endpoint With Entry Types

Manual Source Entry creation will use one API workflow with an entry type such as `structured_row` or `free_form_text` rather than separate endpoints for each manual-entry shape. Both variants are reviewer-entered Source Submissions, while their payloads and processing paths can differ behind the same product-facing contract.
