# Thin Project-Scoped Taxonomy Records For First Import Slice

The first manual-source import slice will persist resolved category paths as real project-scoped taxonomy records instead of plain category and subcategory strings. This keeps imported memory aligned with the PRD requirement that taxonomy decisions belong to a selected Project Workspace and can influence later review, while deferring taxonomy gates, rename workflows, aliases, and learning behavior until later slices.
